# 검색 등록 · SEO 셋업 가이드 — noideapizza.org (2026-07-04)

> 대상: Google 비즈니스 프로필 / Google Search Console / 네이버 서치어드바이저 / 다음 검색등록 / Bing 웹마스터 / Google Analytics
> 이 문서의 모든 URL·계정 경로는 2026-07 기준. 사이트 현황을 실제로 점검한 뒤 작성함.

---

## 🍕 초간단 실행 체크리스트 — 이것만 위에서부터 따라 하세요

한 줄 요약: **홈페이지는 차렸는데 아직 어느 검색엔진 "전화번호부"에도 안 올라간 상태.** 아래는 전화번호부에 올리는 신청서 작성 순서입니다. 각 항목 끝의 §번호를 누르면 이 문서 아래쪽에 상세 설명이 있습니다.

### STEP 0. 준비 — 파일을 실제 사이트에 반영 (5분, 컴퓨터에서 1회)

레포 폴더에서 터미널 열고 한 줄씩:

```bash
git checkout main
git pull
git merge claude/inquiry-flow-seo-setup-g3uvk3
git push
npm run build
npx gh-pages -d dist
```

- [ ] 브라우저에서 `https://noideapizza.org/robots.txt` 열기 → **글자가 보이면 성공** (404면 5분 기다렸다 새로고침)

### STEP 1. 오늘 30분 — 검색엔진 4곳에 사이트 알리기 (전부 무료)

- [ ] **구글** (10분): https://search.google.com/search-console → "URL 접두어"에 `https://noideapizza.org/` 입력 → 자동 인증됨(GA 설치돼 있어서) → 왼쪽 [Sitemaps] 메뉴 → `sitemap.xml` 입력 → 제출 (→ §1)
- [ ] **빙** (5분): https://www.bing.com/webmasters → 구글 계정 로그인 → **"GSC에서 가져오기"** 버튼 클릭 → 끝 (→ §2)
- [ ] **네이버** (15분): https://searchadvisor.naver.com → 사이트 등록 → "메타태그" 인증 선택 → 태그 복사해서 `index.html`의 `<title>` 윗줄에 붙여넣기 → STEP 0의 build/gh-pages 두 줄 다시 실행 → 인증 확인 → [요청>사이트맵 제출]에 `https://noideapizza.org/sitemap.xml` (→ §3)
- [ ] **다음** (5분): https://register.search.daum.net → 카카오 로그인 → 신규등록 → URL 넣고 신청 → 심사 대기(며칠~2주) (→ §4)

### STEP 2. 오늘 신청만 해두기 — 완료까지 1~2주 걸리는 것

- [ ] **구글 비즈니스 프로필**: https://business.google.com → "노아이디어피자 이수본점"부터 등록 → 우편/전화 인증 신청 (인증 우편이 오래 걸리니 **오늘 신청**) (→ §5)

### STEP 3. 10분 — GA(방문자 분석) 마무리 ※ 설치는 이미 돼 있음

- [ ] https://analytics.google.com → ⚙관리 → 이벤트 → `generate_lead` 옆 **"핵심 이벤트" 토글 ON** (→ §6)
- [ ] ⚙관리 → 데이터 보관 → **2개월 → 14개월**로 변경 (→ §6)
- [ ] 직접 사이트에서 테스트 문의 1건 넣고 → [실시간] 보고서에 `generate_lead` 뜨는지 확인

### STEP 4. 매주 월요일 5분 — 성과 확인 루틴

- [ ] 서치콘솔 [실적] → **어떤 검색어로 사람들이 들어왔나** → 잘 나오는 키워드는 광고 문구·랜딩 카피에 재활용
- [ ] GA4 [보고서>획득>트래픽 획득] → **어느 채널에서 온 사람이 문의를 남겼나** → 문의 안 나오는 채널은 광고비 축소

---

## 0. 현재 상태 (실측, 2026-07-04)

| 항목 | 상태 |
|------|------|
| 배포 | GitHub Pages (`gh-pages` 브랜치), 커스텀 도메인 `noideapizza.org`, HTTPS 정상 |
| GA4 | ✅ **이미 설치됨** — `G-T6ZQLZPT36`, `cta_click`·`generate_lead` 이벤트 발화 중 (`index.html`, `src/App.tsx`) |
| robots.txt | ❌ 없었음(404) → **이번 커밋에 `public/robots.txt` 추가됨** |
| sitemap.xml | ❌ 없었음(404) → **이번 커밋에 `public/sitemap.xml` 추가됨** |
| title / description | ✅ 있음 (`index.html`) |
| og / 카카오톡 미리보기 태그 | ❌ 없음 → §8 후속 권장 |
| 구조화 데이터(JSON-LD) | ❌ 없음 → §8 후속 권장 |
| 본문 렌더링 | ⚠️ 전부 JS 렌더(CSR) — 네이버·다음 크롤러가 본문을 못 읽을 수 있음 (§4 주의사항) |
| privacy.html | `noindex` 처리 — 정상 (검색 노출 불필요 페이지) |

**⚠️ 선행 조건: 이 브랜치의 `robots.txt`·`sitemap.xml`이 라이브에 반영되어야 아래 2·3·5번 등록이 진행됨.**
반영 방법 = 기존 배포 방식 그대로:

```bash
git checkout main && git merge claude/inquiry-flow-seo-setup-g3uvk3   # 또는 PR 머지
npm run build
npx gh-pages -d dist        # gh-pages 브랜치 배포 (기존 방식)
# 확인:
curl -I https://noideapizza.org/robots.txt    # 200이면 OK
curl -I https://noideapizza.org/sitemap.xml   # 200이면 OK
```

---

## 추천 진행 순서 (총 소요 기준)

| 순서 | 작업 | 소요 | 비고 |
|------|------|------|------|
| ① | robots/sitemap 배포 (위 명령) | 5분 | 모든 등록의 선행 조건 |
| ② | Google Search Console | 10분 | GA가 이미 있어 인증 원클릭 |
| ③ | Bing 웹마스터 | 5분 | GSC에서 가져오기 한 방 |
| ④ | 네이버 서치어드바이저 | 15분 | 메타태그 추가 → 재배포 1회 필요 |
| ⑤ | 다음 검색등록 | 5분 | 신청만, 심사 수일~2주 |
| ⑥ | GA4 마무리 설정 | 10분 | 핵심 이벤트 지정 + GSC 연결 |
| ⑦ | Google 비즈니스 프로필 | 매장당 20분 + 인증 대기 | 우편 인증 시 1~2주 소요 → **오늘 신청해두는 게 이득** |

---

## 1. Google Search Console (필수 — 유입 키워드 확인)

**주소**: https://search.google.com/search-console

### 1-1. 속성 추가
"속성 추가" 클릭 → 두 가지 유형 중 선택:

| 유형 | 입력 | 인증 방법 | 추천 |
|------|------|-----------|------|
| **도메인** | `noideapizza.org` | DNS TXT 레코드 (도메인 구입처에서 추가) | DNS 접근 가능하면 이쪽 — www 포함 전부 커버 |
| **URL 접두어** | `https://noideapizza.org/` | **GA 태그로 자동 인증** ← 이미 설치돼 있어 원클릭 | DNS가 번거로우면 이쪽이 5분 코스 |

> GA4(`G-T6ZQLZPT36`)를 만든 **같은 구글 계정**으로 로그인하면, URL 접두어 방식에서 "Google 애널리틱스" 인증이 자동으로 통과됩니다. 별도 파일 업로드 불필요.

### 1-2. 등록 직후 할 일 3가지
1. 왼쪽 메뉴 **Sitemaps** → `sitemap.xml` 입력 → 제출
2. 상단 **URL 검사** → `https://noideapizza.org/` 입력 → **색인 생성 요청** (첫 색인을 며칠 앞당김)
3. 2~3일 후부터 **실적(Performance)** 보고서 확인 — **"검색어" 탭이 바로 "어떤 키워드로 유입됐는지"**. 노출수/클릭수/게재순위까지 나옴.

---

## 2. Bing 웹마스터 도구 (ChatGPT 검색 대응)

**주소**: https://www.bing.com/webmasters

1. Microsoft 또는 **Google 계정으로 로그인** 가능
2. 첫 화면에서 **"GSC에서 가져오기(Import from Google Search Console)"** 선택 → 위 1번에서 만든 속성 선택 → 사이트+사이트맵이 **자동으로 등록됨**. 별도 인증 불필요.
3. 끝. (선택: 설정에서 IndexNow 켜면 색인 반영이 빨라짐)

> ChatGPT 검색·Copilot이 Bing 인덱스를 활용하므로, AI 검색에서 "노아이디어피자 공급/B2B" 관련 답변에 노출될 통로입니다. 5분 투자 대비 효율 최상.

---

## 3. 네이버 서치어드바이저 (네이버 웹 검색 노출)

**주소**: https://searchadvisor.naver.com → 우측 상단 "웹마스터 도구"

> ⚠️ **스마트플레이스(지도)와 완전히 별개** 맞습니다. 이수본점 플레이스는 이미 있음(네이버 지도 place 1071004649). 서치어드바이저는 **웹사이트(noideapizza.org)의 네이버 검색 노출**을 관리하는 도구.

### 3-1. 사이트 등록 + 소유 확인
1. 네이버 로그인 → 웹마스터 도구 → **사이트 등록**: `https://noideapizza.org`
2. 소유 확인 — 둘 중 하나:
   - **HTML 파일 방식**: 내려받은 `naver*.html` 파일을 `public/`에 넣고 → `npm run build && npx gh-pages -d dist` 재배포 → 확인 버튼
   - **메타태그 방식(추천)**: 발급된 태그를 `index.html`의 `<head>` 안(예: `<title>` 바로 위)에 붙여넣기 → 재배포 → 확인 버튼
   ```html
   <meta name="naver-site-verification" content="발급받은코드" />
   ```

### 3-2. 등록 직후 할 일 3가지
1. **요청 → 사이트맵 제출**: `https://noideapizza.org/sitemap.xml`
2. **요청 → 웹 페이지 수집**: `https://noideapizza.org/` 수동 수집 요청
3. **검증 → robots.txt**: 정상 수집되는지 확인

### 3-3. ⚠️ 이 사이트만의 주의사항 (CSR)
네이버 크롤러는 자바스크립트 렌더링이 매우 제한적입니다. 이 사이트는 본문 전체가 React(JS)로 그려지므로, **네이버는 사실상 `<title>`과 `<meta description>`만 읽습니다.**
- 지금 상태로도: "노아이디어피자" 브랜드 검색 노출(사이트 제목+설명)은 가능 → 등록 가치 충분
- 본문 키워드("피자 공급", "가맹 없는 피자" 등)로 상위 노출을 노리려면: §8의 og 태그 강화 + 장기적으로 **프리렌더링**(빌드 시 HTML에 본문을 박제, `vite-prerender-plugin` 등) 검토

---

## 4. 다음(Daum) 검색등록

**주소**: https://register.search.daum.net

1. **카카오 계정** 로그인
2. **신규 등록** → "사이트 검색" 선택 → URL `https://noideapizza.org` 입력
3. 사이트 소개 문구 입력(간결하게, 예: "직영점에서 검증한 피자 도우·소스·토핑을 가맹 없이 공급하는 노아이디어피자 B2B 상담 페이지")
4. 제출 → 심사 수일~2주. 등록되면 **다음 통합검색 + 카카오(#검색) 양쪽에 노출** — "한 번 등록으로 둘 다"가 이 의미.

> 소유 확인 절차 없음(신청·심사제). 반려되면 소개 문구를 사실 위주로 고쳐 재신청.

---

## 5. Google 비즈니스 프로필 (지도 + 브랜드 검색 패널)

**주소**: https://business.google.com

### 5-1. 등록 단위
**직영 매장별로 각각** 등록합니다 (프로필 1개 = 실제 방문 가능한 장소 1곳):

| 프로필 | 주소 | 비고 |
|--------|------|------|
| 노아이디어피자 이수본점 | 서울 동작구 동작대로33가길 5, 1층 102호 | ☎ 070-8121-5880, 우선 등록 추천 |
| 노아이디어피자 신사점 | (주소 입력) | |
| 노아이디어피자 목동점 | (주소 입력) | |

본사((주)노아이디어컴퍼니, 여의도)는 방문 손님을 받는 곳이 아니면 등록하지 않는 게 원칙입니다.

### 5-2. 절차
1. 구글 계정 로그인 → 비즈니스 이름 입력 (예: `노아이디어피자 이수본점`)
2. 카테고리: **피자 전문점** (추가: 피자 배달)
3. 주소 입력 → 지도 핀 위치 확인
4. 연락처 `070-8121-5880` + 웹사이트 `https://noideapizza.org` 입력
5. **인증**: 우편엽서(1~2주 소요) / 전화 / 영상 인증 중 구글이 제시하는 방법 → **대기 기간이 있으니 오늘 신청**
6. 인증 완료 후: 영업시간·메뉴 사진·매장 사진(레포 `nip_assets/` 활용)·소개문 채우기. **정보(상호·주소·전화)는 네이버 플레이스와 글자까지 동일하게** — 일관성(NAP)이 로컬 SEO 신호.

> 효과: 구글 지도 노출 + "노아이디어피자" 구글 검색 시 우측 지식패널 + 프로필의 웹사이트 링크가 noideapizza.org의 신뢰 신호가 됨.

---

## 6. Google Analytics — 이미 설치됨, 남은 마무리 4가지

설치는 끝난 상태(`G-T6ZQLZPT36`)이므로 아래만 하면 됩니다. **주소**: https://analytics.google.com

1. **핵심 이벤트 지정**: 관리(⚙) → 데이터 표시 → 이벤트 → `generate_lead` 행의 **"핵심 이벤트로 표시" 토글 ON**
   → 이후 모든 보고서에서 "문의 전환" 기준으로 소스별 성과 비교 가능. (`generate_lead`는 폼 전송 성공 시에만 발화하도록 이미 구현돼 있음 — `src/ContactForm.tsx` → `src/App.tsx trackLead`)
2. **작동 확인**: 보고서 → 실시간 → 본인이 테스트 문의 1건 제출 → `generate_lead` 이벤트가 뜨는지 확인
3. **Search Console 연결**: 관리(⚙) → 제품 링크 → **Search Console 링크** → §1의 속성 연결
   → GA 안에서도 "어떤 검색어로 들어왔는지" 보고서(보고서 → 라이브러리에서 Search Console 컬렉션 게시) 확인 가능
4. **데이터 보관 기간 연장**: 관리(⚙) → 데이터 수집 및 수정 → 데이터 보관 → **2개월 → 14개월**로 변경 (기본값이 2개월이라 탐색 분석 데이터가 일찍 지워짐)

**"방문자가 어떻게 들어왔는지" 보는 곳**: 보고서 → 획득 → **트래픽 획득** (세션 소스/매체별). 광고 집행 시 링크에 UTM을 붙여야 정확히 갈라짐:

```
https://noideapizza.org/?utm_source=instagram&utm_medium=paid&utm_campaign=2607_lead
```

---

## 7. 등록 후 확인 루틴 (주 1회, 5분)

- GSC 실적: 노출 키워드 / 클릭 — **어떤 검색어가 문의로 이어지는 씨앗인지**
- GA4 트래픽 획득: 소스별 세션 + `generate_lead` 수
- 네이버 서치어드바이저 → 리포트 → 수집/색인 현황
- `site:noideapizza.org` 를 구글/네이버/다음/빙에서 검색 → 색인 여부 즉시 확인

---

## 8. 후속 권장 (등록과 별개로 노출 품질을 올리는 것)

### 8-1. og / 카카오톡 미리보기 태그 (우선순위 높음)
현재 og 태그가 없어 **카카오톡·문자로 링크 공유 시 미리보기가 밋밋하게** 뜸 — B2B 특성상 "지인 사장님한테 링크 전달" 경로가 많아 실질 손해. `index.html` `<head>`에 추가:

```html
<meta property="og:type" content="website" />
<meta property="og:url" content="https://noideapizza.org/" />
<meta property="og:title" content="노아이디어피자 | 가맹 없이, 검증된 피자를 내 가게 메뉴로" />
<meta property="og:description" content="가맹비·로열티 0원. 직영점에서 검증한 도우·소스·토핑을 내 상호 그대로 도입하는 B2B 상담." />
<meta property="og:image" content="https://noideapizza.org/og-image.jpg" />
<meta property="og:locale" content="ko_KR" />
<meta name="twitter:card" content="summary_large_image" />
<link rel="canonical" href="https://noideapizza.org/" />
```
og-image는 1200×630 JPG(히어로 상차림 컷 추천)를 `public/og-image.jpg`로. 적용 후 https://developers.kakao.com/tool/debugger/sharing 에서 캐시 초기화+미리보기 확인.

### 8-2. JSON-LD 구조화 데이터
`index.html`에 Organization/LocalBusiness 스키마 추가 → 구글 비즈니스 프로필과 웹사이트를 잇는 신호:

```html
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "Restaurant",
  "name": "노아이디어피자 이수본점",
  "url": "https://noideapizza.org",
  "telephone": "+82-70-8121-5880",
  "servesCuisine": "피자",
  "address": {
    "@type": "PostalAddress",
    "streetAddress": "동작대로33가길 5, 1층 102호",
    "addressLocality": "동작구",
    "addressRegion": "서울",
    "addressCountry": "KR"
  },
  "parentOrganization": {
    "@type": "Organization",
    "name": "주식회사 노아이디어컴퍼니",
    "taxID": "361-86-03473"
  }
}
</script>
```

### 8-3. 프리렌더링 (네이버 본문 수집 대응, 중기 과제)
네이버/다음에서 본문 키워드로 노출을 노린다면, 빌드 시 정적 HTML을 생성하는 프리렌더링 도입 검토. 단일 페이지라 `vite-prerender-plugin`이나 puppeteer 스냅샷(이미 devDependency에 있음) 방식 모두 가벼움. 우선순위는 og 태그·등록 완료 이후.
