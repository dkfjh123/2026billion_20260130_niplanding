# 문의 전환 점검 보고서 — noideapizza.org (2026-07-04)

> **질문 3개에 답하는 보고서**: ① 유저가 문의를 할 방해요소가 있는가? ② 문의를 하는 흐름인가? ③ 핵심요소가 확 들어오는가?
>
> **검토 방법**: 에이전트 36개 가동 — 실제 빌드 후 모바일(390×844)·데스크톱(1280×800) 스크린샷 실측 + 4개 렌즈(전환 방해요소 / 메시지 명확성 / 폼 기술결함 / SEO 준비상태) 병렬 리뷰 + 지적 31건 전수 반박 검증. **검증 통과 24건만 수록, 7건은 과장·오류로 기각**(부록 참조). 기존 점검(20260610 종합보고서, 20260622 셀디렌즈)과 중복되는 지적은 제외.

---

## 한 줄 결론

**"첫 화면과 설득 구조는 합격. 문제는 ① 폼에 리드가 새는 기술 구멍 3개, ② 폼 앞에서 망설이는 사람을 잡아줄 장치 부재, ③ 폼까지 17화면의 긴 여정 속 반복 구간."**

---

## 질문 ①: 문의를 가로막는 방해요소가 있는가? → **있다. 6건.**

### 🔴 리드가 실제로 유실되는 기술 구멍 (폼)

**1. 연락처에 형식 검증이 없다** — `src/ContactForm.tsx:75`
`type=tel` + `required`뿐이라 `010123`, 공백 1칸으로도 제출이 성공한다. 이메일은 선택이라 미입력이 대부분일 텐데, 연락처가 틀리면 **접수된 리드에 영원히 콜백할 수 없다.**
→ 수정: 숫자만 추출해 9~11자리 확인하는 관대한 검증 + `trim()` (엄격한 pattern은 하이픈 유무로 오탐하니 지양)

**2. 전송에 타임아웃이 없다** — `src/ContactForm.tsx:25`
모바일에서 흔한 "연결은 됐는데 응답이 안 오는" 상태(기지국 전환 등)면 **'전송 중…' 버튼이 수십 초간 고착**되고 에러 문구도 안 뜬다. 폼 다 채운 사람이 침묵 속에 이탈하는 경로.
→ 수정: `signal: AbortSignal.timeout(15000)` 한 줄 (+구형 브라우저용 AbortController 폴백). 타임아웃 나면 기존 에러 UI(전화 안내)가 뜨므로 추가 작업 불필요.

**3. 폼이 죽어도 운영자는 모른다** — `src/ContactForm.tsx:46-48`
catch가 에러를 버리고 화면 표시만 한다. Web3Forms 월 250건 한도 초과·API 장애가 나면 **그 기간 방문자 전원이 오류만 보고 이탈하는데 아무 기록이 안 남는다.**
→ 수정: catch에서 `gtag('event','form_submit_error',{...})` 발화 + 에러 문구에 이메일 대체 경로 추가. GA에서 cta_click 대비 generate_lead 급락 여부를 주간 확인.

### 🟠 폼 앞에서 망설이는 사람을 놓치는 심리 마찰

**4. 폼 옆에 전화 대안이 없다** — `src/App.tsx:525` 부근
자영업자 타깃은 폼보다 전화 선호 비중이 높은데, 전화번호는 푸터 회색 글씨·전송 실패 후·완료 화면에만 있다. `index.css:344`에 이 용도로 보이는 `.alt-contact` 스타일이 **정의만 되고 미사용** — 기획됐다 빠진 흔적.
→ 수정: 폼 바로 아래 "폼이 번거로우시면 전화 주세요 — 070-8121-5880" 한 줄 부활 + `placement:'contact_tel'` 계측.

**5. 연락처 입력 순간의 불안 해소 카피가 없다** — `src/ContactForm.tsx:75, 137`
번호를 적으면 무슨 일이 생기는지(몇 번 연락? 영업전화?) 폼 안에 안내가 없다. B2B 리드폼 최대 이탈 지점이 전화번호 입력 직전이다.
→ 수정: 연락처 필드 아래 힌트 1줄(기존 카피 재사용: "전화로 가볍게 확인 후, 원하실 때만 진행합니다") + 버튼 아래 "1영업일 내 연락드립니다".

**6. 모바일 스티키바가 폼 입력을 방해한다** — `src/App.tsx:554-557`
실측 결과 스티키바는 **폼에 도착한 뒤에도 계속 떠서** 하단 입력칸을 가리고, 키보드가 열리면 시야를 더 줄인다. 보조 슬롯 '방문 안내'는 #visit→다시 #contact로 가는 이중 홉이라 기여도 낮다.
→ 수정: #contact가 뷰포트에 있는 동안 스티키바 숨김(기존 IntersectionObserver 재사용) + `safe-area-inset-bottom` 보정. 보조 슬롯의 `tel:` 교체는 A/B 후보.

---

## 질문 ②: 문의를 하는 흐름인가? → **구조는 문의로 수렴한다. 다만 길고, 중간에 새는 문이 있다.**

### 잘 설계된 것 (검증 통과한 강점)
- 필수 입력 3개+동의로 **폼 무게 최소화**, autoComplete·숫자 키패드 대응 완료
- 전송 실패 시 입력값 보존 + 전화 폴백 — 에러가 곧 이탈이 아닌 구조
- 도입 절차 4스텝 + "미리 약속드리는 것" 블록이 **'문의하면 붙잡히나' 불안을 구조적으로 해소** — 폼 제출을 '계약 시작'이 아닌 '구경 신청'으로 리프레이밍 성공
- `generate_lead`가 전송 성공 시에만 발화 — 전환 집계 정확
- `e.currentTarget` 캡처, 중복 제출 방지 등 React 폼 함정 회피 정확

### 흐름의 약점
**A. 폼까지 물리적으로 멀다** — 실측: 모바일 전체 **19.3화면 분량**(CSS 16,276px), #contact는 **17화면 스크롤 지점**(14,332px). 스티키바 덕에 점프는 가능하지만, 순차 독자는 지친다.

**B. 같은 메시지를 3번 읽게 된다** — `src/App.tsx:341-349, 426-442, 445-459`
band·proof·letter 3개 섹션이 전부 "직영점 검증 + 레시피 주인" 단일 메시지. 특히 437행과 452행은 리드인부터 펀치라인까지 **2문장이 통째로 중복**("시판 원팩과 다른 건 하나입니다… 레시피의 주인이 있습니다"). 385행 선언문은 FAQ 76행과 축자 동일.
→ 수정: 437행 펀치라인을 도우 물성 카피로 교체해 letter의 클라이맥스만 남기기 (band 삭제보다 안전 — HACCP 문장이 거기 있어서).

**C. 설득 초반에 밖으로 나가는 문 10개** — `src/App.tsx:212-260`
3번째 섹션(proofnow)의 유튜브 2 + 인스타 팔로우/핸들 + IG 그리드 6이 전부 `target=_blank` 외부 링크. 인앱 브라우저(인스타/카톡 광고 유입)에서 유튜브 탭 = 앱 전환 = 낮은 복귀율. 특히 '인스타그램 팔로우 →'는 폼 제출과 **경쟁하는 별도 전환 목표**를 상단에 노출.
→ 수정: 유튜브는 인페이지 재생으로, 팔로우 버튼은 푸터로, 그리드는 링크 해제. 이미 `track('youtube')` 계측이 있으니 데이터 확인 후 결정해도 됨.

---

## 질문 ③: 핵심요소가 확 들어오는가? → **첫 화면은 합격. 구멍은 3개.**

실측 스크린샷 기준, 모바일 첫 화면(fold)에 헤드라인 3줄 + 서브카피("가맹 없이 — 내 상호, 내 가격 그대로") + CTA 2개 + 스티키바까지 잘림 없이 전부 들어온다. 핵심 차별점(검증된 레시피/가맹 없음/내 상호·내 가격) 중 2개가 fold 안에서 전달된다. **이 부분은 손댈 필요 없다.**

**A. 예비 창업자가 첫 화면에서 소외된다** — `src/App.tsx:177, 202-205`
'간판은 그대로'는 기존 매장 운영자 전용 훅 — 예비 창업자는 지킬 간판이 없다. persona 검색어 4개 중 3개도 '우리 가게' 보유 전제. 폼은 두 타깃을 나란히 받는데 첫 화면은 한쪽만 부른다.
→ 수정: 헤드라인은 유지(의도된 포지셔닝). hero-sub에 병기 1줄("지금 운영 중인 매장에도, 오픈 준비 중인 가게에도") 또는 persona 검색어 1개를 창업자용으로 교체.

**B. '상품 모듈'이라는 내부 용어가 클릭 전 단계에 노출** — `index.html:22, 31`
title·description의 "피자 상품 모듈"은 자영업자의 언어가 아니다. 검색 결과·카톡 공유 미리보기 = 페이지 도착 **전**에 보이는 첫 카피라 비용이 크다. (본문 2곳은 바로 아래 문장이 풀어주고 있어 title/meta만 수정하면 됨.)
→ 수정: title "노아이디어피자 | 가맹 없이 검증된 피자 메뉴 도입 상담", description은 검색어 기반 재작성("가맹비·로열티 0원 — 직영점에서 검증한 피자 도우·소스·토핑을 내 간판, 내 가격 그대로…"). CSR 구조상 **이 두 줄이 네이버 SEO의 사실상 전부**라 SEO 관점에서도 최우선.

**C. 유일한 제3자 인증 'HACCP'이 본문에 매몰** — `src/App.tsx:347`
점주 후기가 없는 현 단계에서 즉시 검증 가능한 유일한 팩트인데 사진 밴드 문단 속 1회 언급뿐. 반면 fact-chips의 "100%"는 측정치가 아닌 방침이라 실팩트 칩('직영 3곳'·'24-48H')의 신뢰까지 희석.
→ 수정: HACCP을 fact-chips 4번째 칩 또는 폼 위 신뢰 라인으로 승격.

---

## 우선순위 정리 (코드 작업 시)

| 순위 | 항목 | 파일 | 작업량 |
|------|------|------|--------|
| **P1** | 연락처 검증 + trim | ContactForm.tsx:75 | 10줄 |
| **P1** | fetch 타임아웃 15초 | ContactForm.tsx:25 | 1줄 |
| **P1** | 실패 GA 이벤트 + 이메일 폴백 | ContactForm.tsx:46 | 5줄 |
| **P1** | 폼 아래 전화 한 줄(.alt-contact 부활) | App.tsx:525 | 5줄 |
| **P2** | 연락처 안심 카피 + 버튼 아래 응답 약속 | ContactForm.tsx | 카피 2줄 |
| **P2** | 폼 진입 시 스티키바 숨김 + safe-area | App.tsx, index.css | 15줄 |
| **P2** | hero-sub 예비창업자 병기 | App.tsx:181 | 카피 1줄 |
| **P2** | title/description 재작성 | index.html:22,31 | 카피 2줄 |
| **P2** | HACCP 칩 승격 | App.tsx:254 | 5줄 |
| **P3** | 중복 펀치라인 정리(437행) | App.tsx | 카피 |
| **P3** | proofnow 외부링크 축소(데이터 확인 후) | App.tsx:212-260 | 판단 필요 |
| **P3** | 완료 화면 '새 문의 작성' 버튼 | ContactForm.tsx:51 | 3줄 |
| **P3** | replyto 추가 | ContactForm.tsx:34 | 1줄 |
| **P3** | cta_click 누락 5곳(proof/footer/hero_visit/sticky_visit/ig_grid) | App.tsx | 각 1줄 |
| **P3** | 미사용 IBM Plex Mono 삭제 + gsap→CSS 대체 | index.html:24-29, App.tsx | 30-40KB gzip 절감 |
| **P3** | select→라디오 칩, 완료화면 포커스 이동, IG alt 개별화 | ContactForm.tsx, App.tsx | 위생 |

**SEO 관련(별도 가이드 문서와 연동)**: og 태그+og-image(가이드 §8-1), JSON-LD(§8-2), 프리렌더링(§8-3, 중기). ⚠️ **주의: 현재 `dist/`는 robots.txt 추가 이전 빌드본 — 배포 전 반드시 `npm run build` 재실행** (가이드 STEP 0 순서대로면 안전).

## 렌더 실측 기타 관찰

- 빌드 정상(JS 340KB/gzip 115KB), 가로 스크롤·레이아웃 깨짐 없음, 로컬 이미지 19개 전부 정상
- 모바일 상단 헤더+하단 스티키바 = 뷰포트의 약 27% 상시 점유
- 유튜브 썸네일 2개가 헤드리스 환경에서 로드 실패(검은 박스) — 샌드박스 네트워크 제한 가능성이 높으나, **실기기에서 한 번 확인 권장** (App.tsx:102,110의 img.youtube.com 외부 의존)
- 스크롤 리빌 애니메이션 요소는 JS 미발동 환경에서 공백 위험 구조(reduced-motion 대응은 돼 있음) — 프리렌더링 도입 시 주의점

## 부록: 기각된 지적 7건 (반박 검증에서 걸러짐)

신뢰성을 위해 기록: ① "데스크톱 중간 CTA 실종"(nav가 sticky라 상시 노출 — 오류) ② "smooth scroll이 폼 도착 지연"(실제 1~2초 수준 — 과장) ③ "히어로가 브랜드 인지 전제"(첫 훅이 브랜드 비의존 — 오류) ④ "공급 품목 불명"(5곳에서 구체화됨 — 오류) ⑤ "'원팩' 미정의"(타깃층에 대중화된 용어 + 광고 소재가 선노출 — 과장) ⑥ "honeypot 오탐 위험"(숨김 체크박스는 자동완성 오탐 사례 없음 — 과장) ⑦ "네이버 인증 메타 부재"(코드 발급 전 당연한 상태 — 무효).
